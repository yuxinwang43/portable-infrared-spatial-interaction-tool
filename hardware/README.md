# Hardware design files

`altium/` contains the Altium schematic and PCB layout for the IR emitter prototype. `enclosure.stl` is the printable casing model.

These files are prototype design sources. Review component values, footprints, electrical safety, and manufacturing constraints before fabricating a device. The repository does not include ESP32 firmware source.
